import React from 'react'

const Dashboard = ({task}) => {
    return (
        <div>
            <h1>Welcome to Dashboard page</h1>
            <h2>what you have to do is {task}</h2>
        </div>
    )
}

export default Dashboard
